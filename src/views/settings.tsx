import Settings from '@/components/Settings';

export default function SettingsView() {
  return (
    <div id="settings-view" className="flex flex-col px-2 flex-1">
      <Settings />
    </div>
  );
}
