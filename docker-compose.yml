version: '3.9'

services:
  platform-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: platform-frontend
    working_dir: /app
    ports:
      - "8080:3000"
    restart: always
